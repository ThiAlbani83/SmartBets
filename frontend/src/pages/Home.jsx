import { useAuthStore } from "../store/useAuthStore";
import { useEffect, useState } from "react";

const Home = () => {
  return (
    <div className="p-4 text-2xl text-primary font-semibold underline">
      Bem vindos ao SmartBets
    </div>
  );
};

export default Home;
